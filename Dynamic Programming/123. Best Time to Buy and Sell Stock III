//MEMOIZATION
class Solution {
private:
    int solve(int i,int buy,vector<int>& prices,int count,vector<vector<vector<int>>>&dp){
        int n=prices.size();
        int profit=0;

        //base case
        if(i>=n || count==2) return 0;

        if(dp[i][buy][count] != -1) return dp[i][buy][count];

        //allowed to buy
        if(buy==1){
            int bought=-prices[i]+solve(i+1,0,prices,count,dp);
            int notbought=0+solve(i+1,1,prices,count,dp);
            profit=max(bought,notbought);
        }

        //not allowed to buy
        if(buy==0){
            int sell=prices[i]+solve(i+1,1,prices,count+1,dp); //transaction completed
            int notsell=0+solve(i+1,0,prices,count,dp);
            profit=max(sell,notsell);
        }
        return dp[i][buy][count]= profit;
    }    
public:
    int maxProfit(vector<int>& prices) {
        int n=prices.size();
        vector<vector<vector<int>>>dp(n,vector<vector<int>>(2,vector<int>(3,-1)));
        return solve(0,1,prices,0,dp);
    }
};
